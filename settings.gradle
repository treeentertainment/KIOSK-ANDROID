pluginManagement {
    repositories {
        google() // ✅ Google 저장소는 여기로 옮김
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "KIOSK-ANDROID"
include ':app'
